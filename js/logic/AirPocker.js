import Rule from './trump_framework/Rule.js';

export default class AirPocker extends Rule {
    constructor() {

    }
    // pockerの組み合わせjsonを用意
    // プレイヤーに25枚配り、そこから強い順にまとめて合計値を出す
    generatePair_()
    sumCard()
}
